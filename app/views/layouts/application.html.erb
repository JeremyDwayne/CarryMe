<!DOCTYPE html>
<html>
  <head>
    <title>CarryMe</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar sticky-top navbar-dark bg-primary navbar-expand-lg">
      <div class="container">
        <%= link_to "CarryMe", root_path, class: "navbar-brand" %>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item <%= active_class(carries_path) %>">
              <%= link_to "Carries", carries_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%# <%= link_to "Carries", carries_path, class: "nav-link" %1> %>
              <a href="#" class="nav-link">Profile</a>
            </li>
          </ul>
          <%= form_with scope: :carry, url: carries_path, method: 'get', local: true, class: "form-inline my-2 my-lg-0" do |f| %>
            <%= f.text_field(:search, placeholder: "Search Carries", id: :search, class: "form-control mr-sm-2") %>
          <% end %>
          &nbsp;
          <% if !user_signed_in? %>
            <%= link_to "Login", new_user_session_path, class: "btn btn-success my-2 my-sm-0" %>
          <% else %>
            <%= link_to "Logout", destroy_user_session_path, method: "delete", class: "btn btn-warning my-2 my-sm-0" %>
          <% end %>
        </div>
      </div>
    </nav>
    <main>
      <div class="container">
        <% flash.each do |name, msg| %>
          <% if msg.is_a?(String) %>
            <div class="alert alert-<%= name.to_s == 'notice' ? 'success' : 'danger' %>">
              <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
              <%= content_tag :div, msg, :id => "flash_#{name}" %>
            </div>
          <% end %>
        <% end %>
        <%= yield %>
      </div>
    </main>
    <footer class="navbar bg-primary fixed-bottom">
      <div class="container">
        &copy; 2019 Jeremy Winterberg
      </div>
    </footer>
  </body>
</html>
